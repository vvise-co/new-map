# =================================
# Frontend Client Environment
# =================================
# For LOCAL DEVELOPMENT only (separate services)
# For unified deployment, use the root .env.example instead

# Your project's backend API URL (only needed for local dev)
# In unified deployment, nginx proxies /api to the backend automatically
NEXT_PUBLIC_API_URL=http://localhost:8080

# Central auth server URL
# NEXT_PUBLIC_* is for client-side (browser redirects to auth server)
# AUTH_SERVER_URL is for server-side (API calls to auth server)
NEXT_PUBLIC_AUTH_SERVER_URL=http://localhost:8081
AUTH_SERVER_URL=http://localhost:8081

# Your app's public URL (used for OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Node environment
NODE_ENV=development
