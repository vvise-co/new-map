# ===========================================
# FRONTEND ENVIRONMENT VARIABLES (Vite)
# ===========================================
# For local development. In production, these are set during Docker build.

# Auth server URL - where users will authenticate
VITE_AUTH_SERVER_URL=http://localhost:8081
